# macOS
.DS_Store

# Local env (secrets)
.env
.env.nostr
.env.nostr.*
.env.clawstr
.env.clawstr.*
nostr/.env.local

# Dependencies
node_modules/

# Optional: temp files from compare script
.remote_list.txt

# Logs / editor
*.log
.cursor/

# Local SQLite (optional / legacy paths under nostr/)
nostr/*.sqlite
nostr/*.sqlite-shm
nostr/*.sqlite-wal
